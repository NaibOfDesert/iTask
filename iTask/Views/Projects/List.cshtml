@model List<Project>
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@{
    ViewData["Title"] = "Projects";
}


@if (SignInManager.IsSignedIn(User)){
 // TODO: add defentdenscies of admin ale signed projects to users.
}


<h1 class="text-dark text-center">Projects</h1>  
<div class="">
 <table class="table text-dark">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">name</th>
                <th scope="col">date start</th>
                <th scope="col">date end</th>
                <th scope="col">status</th>
                <th scope="col">a</th>  

            </tr>


        </thead>
        @foreach(var m in Model){
            <tbody>
                <tr> 
                    <td scope="">@m.Id</td>
                    
                    <td >@m.Name</td>
                    <td scope="">@m.DateStart</td>
                    <td scope="">@m.DateEnd</td>
                    <td scope="">@m.Assignments.Count()</td>
                    <td scope="">                    
                        <a class="" asp-area="" asp-controller="Projects" asp-action="Details" asp-route-id="@m.Id">>>></a>
                    </td>



                </tr>
            </tbody>
        }
    </table>
    <a class="btn btn-primary" asp-area="" asp-controller="Projects" asp-action="Add"> Add Project</a>
</div> 
