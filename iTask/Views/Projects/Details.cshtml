@model ProjectCard

<div class="text-left">
    <h1 class="display-4">@Model.Project.Name</h1>
    
    <table> 
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">name</th>
                <th scope="col">date start</th>
                <th scope="col">date end</th>
                <th scope="col">Task Count</th>
                <th scope="col">a</th>  
            </tr>
        </thead>
        <tbody>
            <tr> 
                <td scope="">@Model.Project.Id</td>
                <td >@Model.Project.Name</td>
                <td scope="">@Model.Project.DateStart</td>
                <td scope="">@Model.Project.DateEnd</td>
                <td scope="">@Model.Project.Assignments.Count()</td>
                <td scope="">                    
                    <a class="" asp-area="" asp-controller="Projects" asp-action="Details" asp-route-id="1">>>></a>
                </td>
            </tr>
        </tbody>
    </table>
     
        


</div>
<a class="btn btn-primary" asp-area="" asp-controller="Projects" asp-action="AddTask" asp-route-id="@Model.Project.Id"> AddTask</a>
<a class="btn btn-primary" asp-area="" asp-controller="Projects" asp-action="Edit" asp-route-id="@Model.Project.Id"> Edit</a>
<a class="btn btn-primary" asp-area="" asp-controller="Projects" asp-action="Remove" asp-route-id="@Model.Project.Id"> Remove Project</a>

